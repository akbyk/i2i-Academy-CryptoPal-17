services:
  postgres:
    image: postgres:15-alpine
    container_name: cryptopal-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      # Sol taraf (.env içindeki POSTGRES_PORT neyse o olur, boşsa varsayılan 5432'ye düşer)
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      # Eğer db/init.sql içinde hata alırsan bu satırı tamamen devre dışı bırakabilirsin
      - ./db/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    networks:
      - cryptopal-net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: cryptopal-redis
    restart: unless-stopped
    ports:
      - "${REDIS_PORT:-6379}:6379"
    networks:
      - cryptopal-net

  cryptopal-core:
    build:
      context: .
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started
    environment:
      - DATABASE_URL=postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}
      - REDIS_URL=redis://redis:6379
    networks:
      - cryptopal-net

networks:
  cryptopal-net:
    driver: bridge

volumes:
  pgdata:
spring:
  application:
    name: CryptoPal

  sql:
    init:
      mode: ${SPRING_SQL_INIT_MODE:never}
      schema-locations: file:db/init.sql
  jpa:
    hibernate:
      ddl-auto: none

  datasource:
    url: ${POSTGRES_URL:jdbc:postgresql://127.0.0.1:5439/cryptopal}
    username: ${POSTGRES_USER:postgres}
    password: ${POSTGRES_PASSWORD:mysecurepassword123}
    driver-class-name: org.postgresql.Driver

  data:
    redis:
      host: ${REDIS_HOST:127.0.0.1}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD}
      ssl:
        enabled: true
    jpa:
      repositories:
        bootstrap-mode: default
      autoconfigure:
        exclude:
          - org.springframework.boot.autoconfigure.data.redis.RedisRepositoriesAutoConfiguration

server:
  port: ${SERVER_PORT:8080}

app:
  security:
    jwt:
      secret: ${JWT_SECRET:supersecretjwtkeyforlocaldevelopment1234567890}
  ai:
    gemini:
      api-key: ${GEMINI_API_KEY:}
      model: ${GEMINI_MODEL:gemini-flash-lite-latest}
      base-url: https://generativelanguage.googleapis.com
      timeout-seconds: 15

cryptopal:
  data-source: binance
  session:
    ttl-seconds: 3600

  market:
    tracked-symbols:
      - BTC
      - ETH
      - SOL
      - AVAX
      - XRP
      - BNB
  binance:
    base-url: https://api.binance.com
    symbols:
      - BTCUSDT
      - ETHUSDT
      - SOLUSDT
      - AVAXUSDT
      - XRPUSDT
      - BNBUSDT
    connect-timeout-ms: 3000
    read-timeout-ms: 5000
  ai:
    context:
      transaction-limit: 15
      price-trend-points: 24